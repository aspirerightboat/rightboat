development:
  clients:
    default:
      database: rightboat_development
      hosts:
        - localhost:27017
      options:
  options:

test:
  clients:
    default:
      database: rightboat_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0

production:
  clients:
    default:
      database: rightboat_v2
      hosts:
        - 172.31.24.106:27017
      options:
  options:

staging:
  clients:
    default:
      database: rightboat_v2
      hosts:
        - localhost:27017
      options:
  options:
