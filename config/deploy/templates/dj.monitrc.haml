check process dj_rightboat with pidfile #{shared_path}/tmp/pids/delayed_job.pid
start program = "#{delayed_job_cmd % {cmd: 'start'}}"
stop  program = "#{delayed_job_cmd % {cmd: 'stop'}}"
if 5 restarts within 5 cycles then timeout
