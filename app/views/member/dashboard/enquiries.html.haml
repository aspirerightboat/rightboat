- content_for :header_text do
  .navbar-h My <strong>Rightboat</strong>

.member-content(data-layout-mode='list')
  %h1 My Enquiries
  %hr
  - if @enquiries.blank?
    .alert.alert-info
      You have no enquiries yet.
  - else
    .row
      - @enquiries.each do |enquiry|
        - if (boat = enquiry.boat)
          = render layout: 'member/shared/boat', locals: {boat: boat, share_enabled: false} do
            .boat-links.boat-links-bottom
              .boat-price
                %span(data-attr-name='price')= boat_price(boat)
                %span.tax-status.small= boat.tax_paid? ? 'Tax paid' : ''
              .text-nowrap.pull-right
                = link_to boat_pdf_path(boat.slug), class: 'pdf-link', target: :blank do
                  %i
                = link_to 'Search Similar', search_path(boat.similar_options(current_currency)), class: 'btn-flat btn-black search-similar'
                = link_to 'Enquire Again', '#', class: 'btn-flat btn-black request-details'
