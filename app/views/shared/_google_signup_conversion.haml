- if Rails.env.production?
  :javascript
    window.google_trackConversion({
        google_conversion_id: 953777258,
        google_conversion_language: "en",
        google_conversion_format: "3",
        google_conversion_color: "ffffff",
        google_conversion_label: "VPtICN6I1WMQ6vjlxgM",
        google_remarketing_only: false
    });

:javascript
  setTimeout(function() {
    $('##{form_name} button[type="submit"]').addClass('inline-loading').prop('disabled', true)
  }, 10);

  setTimeout(function() { // wait some time for google conversion call
    window.location = '#{form_name == :signup_form ? user_area_path(@user) : member_enquiries_path}'
  }, 100);
