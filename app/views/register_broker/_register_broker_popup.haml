#register_broker_popup.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %button.close(aria-label="Close" data-dismiss="modal" type="button")
          %span(aria-hidden="true") &times;
      .modal-body
        %h2 Register as Broker
        = form_tag register_broker_path, class: 'simple-ajax-form', remote: true, data: {type: :json} do
          .row
            .col-xs-12
              %p To register as a Broker and be able to list boats for sale on Rightboat, you must be a professional yacht broker.
          .row
            .col-xs-12
              %p
                Please confirm:<br>
                %input(type="checkbox" name="agree" id="agree")
                %label(for="agree")
                  I am a professional boat broker and wish to register for FREE as a lifetime member of Rightboat.com.
                  and I acknowledge that by registering and using Rightboat.com, I agree to be bound by the
                  #{link_to 'Rightboat.com terms and conditions', toc_path}
          .row
            .col-sm-2.col-xs-12
              .form-group
                = select_tag :title, options_for_select(User::TITLES, current_user.try(:title)), prompt: 'Title', class: 'select-title'
            .col-sm-4.col-xs-12
              .form-group
                = text_field_tag :first_name, nil, placeholder: 'First Name', class: 'form-control', 'data-validetta' => 'required'
            .col-sm-6.col-xs-12
              .form-group
                = text_field_tag :last_name, nil, placeholder: 'Last name', class: 'form-control', 'data-validetta' => 'required'
          .row
            .col-sm-6.col-xs-12
              .form-group
                = email_field_tag :email, '', placeholder: 'Email', class: 'form-control', 'data-validetta' => 'required,email'
            .col-sm-6.col-xs-12
              .form-group
                = phone_field_tag :phone, '', placeholder: 'Phone', class: 'form-control', 'data-validetta' => 'required'
          .row
            .col-sm-6.col-xs-12
              .form-group
                = password_field_tag :password, '', placeholder: 'Password', class: 'form-control', 'data-validetta' => 'required'
            .col-sm-6.col-xs-12
              .form-group
                = password_field_tag :password_confirmation, '', placeholder: 'Repeat Password', class: 'form-control', 'data-validetta' => 'required'
          .row
            .col-xs-12
              .form-group
                = text_field_tag :company_name, '', placeholder: 'Company Name', class: 'form-control', 'data-validetta' => 'required'
          .row
            .col-xs-12
              %p
                FREE membership is the first step to listing your boats on Rightboat.com.
                We do not charge for listing boats, only for leads supplied.
                Register now for FREE membership and to find out more.
          .row
            .col-xs-12
              %button.btn-flat(type="submit") Create Account
              %span.or Or
              %a(href="#" aria-label="Close" data-dismiss="modal") Cancel
          .row
            .col-xs-12
              %p
                Do you have an account already?
                = link_to 'Login', '#', class: 'user-login'
