- if Rails.env.production?
  :javascript
    window.google_trackConversion({
        google_conversion_id: 953777258,
        google_conversion_language: "en",
        google_conversion_format: "3",
        google_conversion_color: "ffffff",
        google_conversion_label: "G6jvCOilq2IQ6vjlxgM",
        google_remarketing_only: false,
        google_conversion_currency: "GBP",
        google_conversion_value: #{Rails.env.development? ? 0 : lead_price},
    });

- if local_assigns[:redirect_to_enquiries]
  :javascript
    setTimeout(function() {
      $('.enquiry-form button[type="submit"]').addClass('inline-loading').prop('disabled', true)
    }, 10);

    setTimeout(function() { // wait some time for google conversion call
      window.location = '#{member_enquiries_path}'
    }, 100);
