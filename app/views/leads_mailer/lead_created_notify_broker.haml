- boat_url = boat_url(@boat.slug)

%p Dear #{@broker.try(:contact_name) || @broker.name}
%p We are pleased to inform you that you have an enquiry from #{link_to 'Rightboat', root_url}.

%p The client details are as follows:
%ul
  %li <b>Name:</b> #{@enquiry.name}
  - if @boat.user.customer_detail_requested?
    %li <b>Customer Email:</b> #{@enquiry.email}
    - unless @enquiry.phone.blank?
      %li <b>Customer Tel:</b> #{@enquiry.country_code} #{@enquiry.phone}
    %li <b>Customer message:</b> #{@enquiry.message}

%p Boat details:
%ul
  %li <b>Make:</b> #{@boat.manufacturer}
  %li <b>Model:</b> #{@boat.model}
  %li <b>Details page:</b> <a href="#{boat_url}">#{boat_url}</a>
  %li <b>RB Ref:</b> #{@boat.ref_no}
  %li <b>Broker source ID:</b> #{@boat.source_id}

%p #{@enquiry.created_at.strftime('%d %b %Y')}

%p Please login to your #{link_to 'broker account', lead_url(@enquiry)} to view the full customer details

%br
%p
  Kind regards
  %br
  The #{link_to 'Rightboat', root_url} Team
